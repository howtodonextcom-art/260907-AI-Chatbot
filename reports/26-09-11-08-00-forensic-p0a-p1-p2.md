# Forensic Remediation — P0-A / P1 / P2 (không Dual Judge)

- **Time:** 2026-09-11 08:00 (UTC+7)
- **Prompt:** Forensic Remediation Brief (user-approved; rejected original 82→100 dual-judge / LangGraph rewrite)
- **Commit:** không commit (user không yêu cầu)

## 1. Forensic matrix — claim prompt gốc

| Claim trong prompt gốc | Verdict | Bằng chứng trong repo (đối chiếu code) |
|---|---|---|
| Semantic Dedup engine (near-paraphrase) | **PARTIAL → đóng hẹp ở P2** | Trước: chỉ exact-string `norm()` / `dedupeByKey`. Nay: `entity-cluster.ts` Jaccard + char 3-gram, ngưỡng 0.8. **Cấm** silent resolve HIGH. |
| Dual-Agent Cross-Consensus Judge + BiasMetric | **REJECT / OUT OF SCOPE** | Trùng lớp với SecondOpinion (DeepSeek, blind) + `Judge.secondOpinionAgreement` + `deriveAgentAgreement` (P0-02). Thêm judge thứ hai / “fairness LLM pass” = fake consensus + đốt budget. Không có runtime evidence rằng P0-02 còn hở. |
| Non-blocking event checkpointing toàn pipeline / LangGraph | **PARTIAL — không rewrite** | Đã có: auto-workflow resume từ `workflow.completedStages`; Firestore `runTransaction` trên update. Thiếu (đã có từ phiên trước): `maxDuration` + persist ngay sau Parallel Frame. Không cần event-sourced orchestrator. |
| Exact-string dedupe khi merge parallel frames | **CONFIRM — đã có** | `parallel-frame-merge.ts` `seenA/seenU/seenC/seenO`. Giữ nguyên; P2 chạy *sau* bước này. |
| Soft Gate: HIGH Unknown không PAUSE trước OPTIONS→PREPARE | **CONFIRM — đã có** | `collectPipelineBlockers()` chỉ `EVIDENCE_CONTRADICTION`. HIGH chặn `DECISION_READY`/`DECIDED` qua `canEnterDecisionReady`. |
| SecondOpinion + Judge agreement | **CONFIRM — giữ nguyên** | Không đụng schema P0-02, không thêm BiasMetric. |
| Framer quorum ≥2; Conflict Engine deterministic; humanApproveProof | **CONFIRM — không đổi** | Không thay Conflict Engine bằng LLM synthesizer. |
| Firestore atomic updates | **CONFIRM — không đổi** | `runTransaction` trên các `update()` path. |

## 2. P0-A — CONTRADICTED → DECISION_READY

**Đã đóng từ phiên trước** (`[[contradicted-decision-ready-gap]]`). Phiên này **không nhân bản gate**. Đã xác nhận lại 4 call site đều lấy `contradictedAssumptionCount` từ **danh sách live đã merge**, không snapshot cũ:

| Call site | Nguồn count |
|---|---|
| `canEnterDecisionReady` / `gateStatusTransition` | field bắt buộc trên context |
| `applyAnalystState` | `assumptions` vừa merge + cluster |
| Judge ACCEPT / ACCEPT_WITH_CHANGES | `mergedAssumptions` |
| PATCH `/api/sessions/:id` | `patch.assumptions ?? session.assumptions` |
| `applyParallelFrameState` | assumptions sau cluster (đề xuất VALIDATING, không DECISION_READY) |

Test sẵn có: `transition-gate.test.ts` (reject khi `contradictedAssumptionCount: 1`; `applyAnalystState` bỏ qua `suggestedStatus=DECISION_READY` khi assumption CONTRADICTED); `core.test.ts` `canEnterDecisionReady` false khi count=1.

## 3. P1 — Run durability

**Đã đóng từ phiên trước** (`[[parallel-frame-early-persist]]`). Không ghi thừa:

- `src/app/api/sessions/[sessionId]/run/route.ts` — `export const maxDuration = 300`
- `decision-orchestrator.ts` — `repos.sessions.update(merged.patch)` ngay sau `applyParallelFrameState`, **không** gate `throwIfAborted()`
- Test: `automatic-workflow.test.ts` “persists the Parallel Frame merge immediately, before the generator finishes”

Không persist “sau mỗi stage” mù quáng: FRAME là stage duy nhất cùng tick với 2–3 LLM billed calls; CRITIC/JUDGE/VERIFY không chạy cùng tick với Parallel Frame.

## 4. P2 — Near-dup clustering (mới trong phiên này)

File mới: `src/domain/decision/entity-cluster.ts`

- Fingerprint = content words + char 3-gram; Jaccard ≥ `HIGH_SIMILARITY_THRESHOLD` (0.8)
- Unknown: `importance = max`; nếu **bất kỳ** member còn blocking (HIGH + non-terminal) → survivor **phải** non-terminal (ưu tiên `OPEN`); không bao giờ nuốt HIGH OPEN vào RESOLVED
- Assumption: `CONTRADICTED` thắng; không nâng `UNVERIFIED` → `SUPPORTED`
- Tích hợp **sau** exact-string merge: `applyParallelFrameState`, `applyAnalystState`
- Không LLM auto-dedup; không UI merge-suggest (chấp nhận merge máy khi similarity cao + fail-safe HIGH)

### P2+ (hẹp)

`RESOLVE_WITH_EVIDENCE` đòi evidence `supportsUnknownIds` chứa `unknown.id` **hoặc** trùng token/chủ đề với câu hỏi (`evidenceTopicallySupportsUnknown`). Không stack NLP.

## 5. Dual Judge / BiasMetric — lý do bác bỏ

P0-02 đã sửa lỗ hổng ngữ nghĩa: SecondOpinion **không thấy** Analyst (chạy song song, mù), nên không được tự chấm `agreementScore`. Judge — agent duy nhất thấy cả hai — gán `secondOpinionAgreement` (LOW/MEDIUM/HIGH); `deriveAgentAgreement` map label → số và gắn `agentAgreementMethod: "JUDGE_HEURISTIC" | "UNAVAILABLE"`.

Thêm Dual-Agent Cross-Consensus Judge + BiasMetric sẽ:

1. **Trùng lớp** với SecondOpinion + Judge agreement đã có
2. **Fake consensus** nếu “judge 2” cùng nhà (Gemini) hoặc fallback cùng model
3. **Đốt budget** (thêm 1–2 LLM calls/DEEP tick) mà không có evidence runtime rằng P0-02 còn hở
4. Vi phạm hard constraint: không thêm LLM pass “chấm fairness” lên JudgeDraft

**Không implement.** Giữ SecondOpinion semantics hiện tại.

## 6. P0-B — Firestore rules/indexes deploy (ops, không giả “đã fix”)

Code `src/infrastructure/firebase/rules/firestore.rules` + `firestore.indexes.json` đã đúng (gồm bất biến `ownerId` trên `workspaces`, P0-03) và đã test qua emulator (`pnpm test:rules`). **Chưa deploy lên project thật `chatai-62ca2`.**

Blocker: `firebase deploy --only firestore:rules,firestore:indexes` trả 403 — service account trong `service.json` thiếu IAM `Firebase Rules Admin` / `Cloud Datastore Index Admin`. Đây là blocker quyền hạn ngoài repo, không phải lỗi code. Phiên này **không** chạy `firebase deploy` và **không** tuyên bố production rules đã cập nhật.

Cách gỡ (người vận hành): cấp role trên GCP IAM, hoặc `firebase login` bằng tài khoản Owner/Editor rồi `firebase deploy --project chatai-62ca2 --only firestore`.

## 7. Verify

| Check | Kết quả |
|---|---|
| `pnpm typecheck` | pass (`tsc --noEmit`) |
| `pnpm test` | **226/226** (28 file) — gồm 13 test `entity-cluster.test.ts` + 1 test topical P2+ trong `unknown-policy.test.ts` |
| Soft Gate (integration) | `automatic-workflow.test.ts`: PREPARE + `judgeDraft` khi HIGH còn OPEN; DECISION_READY chỉ sau resolve. Judge ACCEPT bị `transition.rejected` khi HIGH mở. |
| Dual Judge | Không có role/provider/call site mới. Orchestrator vẫn Analyst ∥ SecondOpinion → Critic → Judge. |
| MCP browser | **Blocked by auth** — xem §8 |

## 8. MCP browser (bắt buộc, không bịa credential)

- Dev server: `pnpm dev` → **http://localhost:3000** (Ready 4.6s). Tab sẵn có `http://localhost:3001/` không phải server phiên này.
- `cursor-ide-browser` view `0ffb7a`: homepage → `/workspaces`. UI hiện nút **Đăng xuất** (cookie/session client còn) nhưng `GET /api/workspaces` trả **Invalid authentication token** (401 từ `src/infrastructure/api/auth.ts`).
- Session cũ Mega 6/45 (`/sessions/DCBcWXlvv4YPj8w8smAm`) cũng 401 — không click được “Bắt đầu phân tích”.
- **Không** invent credential. Soft Gate / no-Dual-Judge đã xác nhận qua unit+integration (scripted providers), không qua live Gemini/Groq/DeepSeek trong phiên này.

## 9. Cố ý KHÔNG làm

- Dual Judge / BiasMetric / LLM fairness pass
- LangGraph / event-sourced orchestrator rewrite
- Semantic auto-resolve / silent drop HIGH Unknown
- Persist sau mọi stage (thừa; FRAME đã early-persist)
- Deploy Firestore rules (P0-B IAM)
- UI “gợi ý merge” (merge máy conservative là đủ cho P2)
- Auto-DECIDED, empty-resolve, client `origin=HUMAN_APPROVE`
- Đổi Conflict Engine sang LLM synthesizer
- Đổi SecondOpinion semantics
- Tuyên bố 100/100

## 10. Rủi ro còn lại

- Jaccard 0.8 có thể bỏ sót paraphrase xa (cố ý — ưu tiên không nuốt blocker hơn là gộp tối đa)
- Transitive union-find có thể nối chuỗi A~B~C; fail-safe HIGH vẫn giữ blocking
- P2+ token overlap có thể từ chối evidence đúng nhưng wording lệch nhiều (user vẫn gắn `supportsUnknownIds`)
- P0-B: production Firestore rules cũ vẫn chạy nếu ai ghi thẳng DB
- `canEnterDecisionReady` vẫn chưa chặn assumption status khác CONTRADICTED theo nghĩa “chỉ SUPPORTED” — ngoài phạm vi brief
